mutation UpdateBeamPack(
  $code: String!
  $name: String
  $description: String
  $image: String
  $start: DateTime
  $end: DateTime
  $flags: [BeamFlagInputType!]
  $packs: [BeamPackInput!]
) {
  UpdateBeamPack(
    code: $code
    name: $name
    description: $description
    image: $image
    start: $start
    end: $end
    flags: $flags
    packs: $packs
  )
}
